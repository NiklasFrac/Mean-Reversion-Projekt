# runs/results

This directory stores generated backtest, calibration, Bayesian optimization,
and reporting outputs.

It is kept in the repository only as a directory scaffold. Concrete runtime
outputs are intentionally ignored unless they are explicitly committed for
audit or review.
