"""
Universe construction package.

This file exists to make `src/universe` a regular Python package (non-namespace),
which avoids mypy module-duplication issues and improves tooling determinism.
"""
